# Copy to .env (Vite loads .env automatically). Never commit real secrets here —
# this one only holds a public API base URL, not a key.

# Base URL of the backend API
VITE_API_URL=http://localhost:4000
